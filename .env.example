# Keep the app on mock data while there is no backend.
EXPO_PUBLIC_SMOKE_RADAR_API_MODE=mock

# Later, when a backend is ready:
# EXPO_PUBLIC_SMOKE_RADAR_API_MODE=real
# EXPO_PUBLIC_SMOKE_RADAR_API_URL=https://your-smoke-radar-api.example.com
